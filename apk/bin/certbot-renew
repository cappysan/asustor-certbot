#!/usr/bin/env sh
#
# NAME
#      certbot-renew - renew the certificates
#
# COMMAND LINE
#
#   --force-renewal    Force the renewal of the certificate
#
. /usr/local/AppCentral/cappysan-certbot/.env.install
cd ${APKG_PKG_DIR:-/nonexistent} || exit 1

function logger() {
  echo "${@}" >&2
  syslog --log 0 --level 0 --user SYSTEM --event "${@}"
}

if test ! -f "${APKG_CFG_DIR}/active"; then
  logger "[Certbot] Certbot certificates are deactivated."
  exit 0
fi

# last_run records last success
if test ! -f ${APKG_CFG_DIR}/last_run; then
  echo "${APKG_CFG_DIR}/last_run does not exist, running init script" >&2
  exec /usr/local/AppCentral/cappysan-certbot/bin/certbot-init "${@}"
fi
echo "${APKG_CFG_DIR}/last_run exists, running renew script" >&2

${APKG_PKG_DIR}/CONTROL/install-hooks.sh

logger "[Certbot] Requesting certificate renewal..."
echo /usr/local/AppCentral/cappysan-certbot/letsencrypt/bin/certbot renew --no-random-sleep-on-renew --pre-hook "run-parts ${APKG_CFG_DIR}/letsencrypt/renewal-hooks/pre/" --post-hook "run-parts ${APKG_CFG_DIR}/letsencrypt/renewal-hooks/post/" "${@}" >&2
if /usr/local/AppCentral/cappysan-certbot/letsencrypt/bin/certbot renew --no-random-sleep-on-renew "${@}" --pre-hook "run-parts ${APKG_CFG_DIR}/letsencrypt/renewal-hooks/pre/" --post-hook "run-parts ${APKG_CFG_DIR}/letsencrypt/renewal-hooks/post/" 2>&1 | tee /tmp/certbot.log ; then
  if grep -q 'Certificate not yet due for renewal' /tmp/certbot.log; then
    logger "[Certbot] Rewewing certificate: Pass. Certificate not due for renewal."
  elif grep -q 'too many requests' /tmp/certbot.log; then
    logger "[Certbot] Rewewing certificate: Failure. Too many requests."
  else
    logger "[Certbot] Renewing certificate: Success."
    date > ${APKG_CFG_DIR}/last_run
  fi
else
  echo "${@}" >&2
  syslog --log 0 --level 1 --user SYSTEM --event "Renewing certificate: Failure"
fi
